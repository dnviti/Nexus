# Nexus Framework Example Application Requirements
# Dependencies for running the complete example application
# Last updated: 2024

# Core Framework (assuming local installation)
# Install the framework from the parent directory:
# pip install -e ..

# WebSocket Support (for Notification Plugin)
websockets==12.0
python-socketio==5.10.0

# File Storage Support
aiofiles==23.2.1
python-magic==0.4.27

# Analytics and Monitoring
prometheus-client==0.19.0

# Database Drivers (choose based on your needs)
# For the example, we use SQLite which is included in Python
# Uncomment if using other databases:
# asyncpg==0.29.0         # PostgreSQL async driver
# aiomysql==0.2.0         # MySQL async driver
# motor==3.3.2            # MongoDB async driver

# Redis for caching and session storage
redis==5.0.1

# Background Tasks (optional)
apscheduler==3.10.4

# HTTP Client for testing
httpx==0.25.2

# Additional utilities
python-dateutil==2.8.2
pytz==2023.3.post1

# Development and Testing
pytest==7.4.4
pytest-asyncio==0.23.3
pytest-cov==4.1.0
httpie==3.2.2             # For manual API testing
locust==2.20.0            # For load testing

# Documentation serving (optional)
mkdocs==1.5.3
mkdocs-material==9.5.3
